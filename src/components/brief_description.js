import React from "react";

export default function brief_description(props) {
  return (
    <div className="brief_description">
      <p className="brief_description_text"> {props.wiki}</p>
    </div>
  );
}
